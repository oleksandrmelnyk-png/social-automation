spring:
  application:
    name: social-automation
  profiles:
    active: prod
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
server:
  servlet:
    context-path: /api
proxy:
  accounts-per-proxy: 5
