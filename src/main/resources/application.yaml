spring:
  application:
    name: social-automation
  profiles:
    active: dev
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
server:
  servlet:
    context-path: /api
app:
  props:
    accounts-per-proxy: 3
    accounts-password: Qwerty1234@
    processing-threads: 0